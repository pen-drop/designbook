import { Meta } from '@storybook/addon-docs/blocks';
import { SectionsOverview } from '../source/components/index.js';
import '../source/index.css';

<Meta title="Sections/Overview" />

# Sections

Each roadmap section gets its own specification — defining user flows, UI requirements, and scope.
Use the AI command to shape a section step by step — the results are displayed here automatically.

---

<SectionsOverview />
