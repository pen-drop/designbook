---
name: designbook-data-model-drupal
description: Guidelines for creating Drupal-compatible data models. Load this skill when DESIGNBOOK_BACKEND is drupal.
---

# Designbook Data Model — Drupal

Drupal-specific rules for data model creation. Provides field naming conventions, entity type mapping, and `composition` guidance for Drupal backends.

## Rules

- [drupal-data-model.md](rules/drupal-data-model.md) — Entity mapping, `composition` per bundle, `field_` prefix convention (loaded during `create-data-model`)

## Resources

- [drupal-views.md](resources/drupal-views.md) — Drupal Views as `config.view.<name>` entities with `view_modes`, templates, and `rows` sample data
