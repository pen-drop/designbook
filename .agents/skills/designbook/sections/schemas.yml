# Section concern schemas

Section:
  type: object
  required: [id, title]
  properties:
    id: { type: string }
    title: { type: string }
    description: { type: string }
    status: { type: string }
    order: { type: integer }
    group: { type: string }
