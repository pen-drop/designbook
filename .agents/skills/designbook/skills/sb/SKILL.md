---
name: sb
description: >
  Start, stop, restart, or inspect the Storybook dev server. Use when the user
  wants to manage the Storybook server for a designbook project.
---

Load the [Storybook CLI reference](../../resources/cli-storybook.md). Dispatch the requested server command directly; default to `storybook status`.
