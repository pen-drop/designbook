---
name: css-generate
description: >
  Generate CSS token files from design tokens (framework auto-selected via DESIGNBOOK_FRAMEWORK_CSS). Use when the user mentions CSS generation, `*.src.css`, or `*.jsonata` token compilation.
---

For this request, follow [intake](resources/intake.md).
