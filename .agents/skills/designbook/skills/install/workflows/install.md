---
title: Install Designbook
description: >-
  Install designbook into an existing project — detect the backend, find the
  target, write config, set up Storybook, and verify the result.
stages:
  config:
    steps:
      - write-config
  storybook:
    steps:
      - setup-storybook
  verify:
    steps:
      - verify-install
---

Template for the planning agent. Use the ordered steps as building blocks. Enumerate repeated targets during intake and write each concrete task explicitly; these stages do not execute or expand at runtime.
