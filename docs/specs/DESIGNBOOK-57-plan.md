# DESIGNBOOK-57 — Implementation plan

Status: proposed; human confirmation pending. Execute only in the coding state after confirmation of the [design](DESIGNBOOK-57-design.md) and [test plan](DESIGNBOOK-57-tests.md).

- [ ] Load Matt's `writing-for-agents`, `designbook-skill-creator`, its common/writing rules, and each matching file-type rule before authoring. Load `designbook-addon-skills` if addon TypeScript changes are necessary. Re-check the declared base branch for prior implementation using the coding workflow's prior-art gate.
- [ ] Inventory active `create-component`/`create-scene` references across core, Drupal, Tailwind, Stitch, templates including import, blueprints, discovery, tests, and instructional examples. Record the two rename targets separately from the retained `create-scene-file` initializer. Resolve every active caller in the final change.
- [ ] Rename the SDC component task to `write-component` and the shared scene task to `write-scene`. Specify selected target, existing content, complete results, preservation, and consumer obligations using shared types. Remove creation-only append/default-regeneration semantics. Put procedural framework guidance in its blueprint/resource home. Declare affected variant stories and optional scripting outputs explicitly.
- [ ] Update the four short SKILL indexes/descriptions, their intake resources, and workflow templates for creation and changes. Preserve design-component's explicit invocation boundary. Freeze identities, deltas, preserved content, references or text criteria, dependencies, usages, build/render checks, and outputs through the existing builder. Select initialization only for absent files.
- [ ] Update shared/integration rule triggers and blueprint references atomically. Ensure screen/shell constraints follow the actual scene target, including component-consumer edits. Preserve schema/validator sharing. Make reference-dependent rules usable with text-only changes, and keep component inventory refresh within the already declared scope.
- [ ] Adjust shared mapping/sample-data contracts only where needed to preserve selected entity/bundle/view- or form-mode boundaries. Retain existing previews and sufficient components/data. Preserve repair/design-verify responsibilities.
- [ ] Strengthen the four current creation cases and author four update cases plus explicit fixture layers in `fixtures/drupal-petshop/`. Use valid current artifacts independently of earlier runs. Cover multiple variants, a prop/slot consumer change, two neighboring scenes plus metadata, navigation/mobile behavior and a consuming screen, and entity-mode isolation. Include the second identical screen update inside its own case without rebuilding between its two runs.
- [ ] Extend the single eval scorer only as required for bounded unchanged-file/baseline access and repeated-run evidence. Make content, reference, required-task/result, and actual build/render checks decisive. Add focused automated coverage for changed scorer behavior and discovered rule/schema contracts; use no second evaluator in the addon.
- [ ] Validate modified skill roots with the skill-creator validator and run the selected focused contract checks. Execute all eight `debo-test run` invocations from this ticket worktree, each in a freshly rebuilt isolated workspace. Compare every saved definition before/after execution and collect summaries, artifact validation, case assertions, builds, and browser results.
- [ ] Resolve failures within the approved scope, rerun affected checks, and report failed or unexecuted cases explicitly. Run `pnpm check` successfully before an implementation commit. Publish per-AC evidence with command, observed result, immutable commit, workspace/workflow locations, and actual build/render observations; transient screenshots/logs stay in the ticket's artifact root.

Completion requires AC1–AC7, not merely eight invocations or file existence. This document creates no fixtures, selectors, test implementation, execution workflow, or environment.
