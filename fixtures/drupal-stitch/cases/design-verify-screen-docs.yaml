fixtures:
  - vision
  - data-model
  - tokens
  - css-generate
  - sections
  - design-shell
  - design-verify-shell
  - design-screen-docs

prompt: |
  You are working in the directory {{workspace}}.
  Run the "/debo design-verify docs" workflow. 
  Verify the docs section.
  Execute the workflow now and Do NOT ask any questions.

assert:
  - type: javascript
    value: output.completedWorkflows['design-verify']?.state?.status === 'completed'
  - type: javascript
    value: Object.values(output.completedWorkflows['design-verify']?.state?.tasks || {}).every(t => t.status === 'done')
  - type: javascript
    value: Object.keys(output.pendingWorkflows).length === 0
