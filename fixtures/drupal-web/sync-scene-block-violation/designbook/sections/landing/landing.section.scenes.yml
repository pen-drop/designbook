id: landing
title: Landing
description: >
  Landing page (Layout Builder) whose content places a block_content hero AND node.promo as a
  block. node.promo has no block_content/block_plugin backing, so the block-decision rule cannot
  type it — sync-to must report and stop. AUTHORED, PENDING LIVE VALIDATION.
status: planned
order: 1
group: Designbook/Sections/Landing
scenes:
  - name: landing
    section: landing
    items:
      - scene: design-system:shell
        with:
          content:
            - entity: block_content.hero
              view_mode: default
              record: 0
            - entity: node.promo
              view_mode: teaser
              record: 0
