id: landing
title: Landing
description: >
  Landing page (Layout Builder): the shell whose content carries the page's blocks — a
  block_content hero and the landing_teasers View rendered beside it as a views_block
  (block_plugin). Both block kinds appear in one Scene; the node.landing edit form is a
  template: presenter surface. Follows the DESIGNBOOK-38 sync-verify-scene pattern: this is a
  Layout-Builder page-content Scene (the page node.landing is implicit — its full display carries
  these blocks, and a bare node is seeded), NOT a design-screen scene, so the design-screen
  route-bearer invariant (screen-scene-constraints, gated to design-screen:create-scene) does not
  apply. AUTHORED, PENDING LIVE VALIDATION.
status: planned
order: 1
group: Designbook/Sections/Landing
scenes:
  - name: landing
    section: landing
    items:
      - scene: design-system:shell
        with:
          content:
            - entity: block_content.hero
              view_mode: default
              record: 0
            # the landing_teasers View placed beside the main content = a views_block (block_plugin);
            # a View node is self-contained (no record / view_mode).
            - entity: view.landing_teasers
