## 1. Schema & Token Data

- [x] 1.1 Extend `design-tokens.schema.yml` to validate `$type: typography` composite tokens — `$value` must be an object with optional `fontFamily`, `fontSize`, `lineHeight`, `fontWeight`, `letterSpacing` keys when `$type` is `typography`
- [x] 1.2 Add `$type: typography` composite tokens to `test-integration-drupal/designbook/design-system/design-tokens.yml` — add h1, h2, h3, body-lg, body, small, caption tokens under the typography group

## 2. Renderer Registry & Architecture

- [x] 2.1 Create the `TOKEN_RENDERERS` registry object in `DeboDesignTokens.jsx` mapping each `$type` to its renderer component
- [x] 2.2 Add `getDominantType(group)` helper that determines the most common `$type` among tokens in a group
- [x] 2.3 Refactor `DeboDesignTokens` main export to use the registry — replace hardcoded `color`/`typography` name checks with type-based dispatching via the registry

## 3. Typography Refactor

- [x] 3.1 Remove the hardcoded `TYPE_SCALE` constant
- [x] 3.2 Refactor `DeboTypographySection` to render `$type: fontFamily` tokens as font cards (existing) and `$type: typography` tokens as a data-driven type-scale table sorted by fontSize descending
- [x] 3.3 Update `DeboTypeScale` to accept an array of typography composite tokens and render preview text with actual fontFamily/fontSize/fontWeight/lineHeight from token `$value`

## 4. Tier 1 Renderers

- [x] 4.1 Create `DeboDimensionSection` — proportional horizontal bars scaled to the max value in the group, with token name and value labels
- [x] 4.2 Create `DeboShadowSection` — card elements with live `box-shadow` applied, showing token name, description, and CSS value

## 5. Tier 2 Renderers

- [x] 5.1 Create `DeboNumberPreview` — opacity square for 0–1 range values, numeric badge with proportional bar otherwise
- [x] 5.2 Create `DeboFontWeightPreview` — "Aa" text rendered at the token's font-weight with numeric label
- [x] 5.3 Create `DeboGradientStrip` — full-width bar with the CSS gradient value applied as background
- [x] 5.4 Create `DeboBorderPreview` — horizontal line with the token's CSS border value applied

## 6. Skill Update — Intake

- [x] 6.1 Update `intake.md` in `designbook-tokens` skill — add a new step after font selection asking the user about their desired type-scale style (e.g., modular scale Major Third 1.25, custom sizes, or skip)
- [x] 6.2 Update `intake.md` — add a new step asking the user if they need breakpoints (yes/no, how many steps)
- [x] 6.3 Update `intake.md` params to include `type_scale: {}` and `breakpoints: {}` — stores user preferences for downstream use by the CSS framework skill

## 7. Skill Update — CSS Framework

- [x] 7.1 Update `designbook-css-tailwind` skill to handle `$type: typography` composite tokens — the CSS framework skill determines the concrete token names (h1, h2, body-lg, etc.) based on the intake type-scale preference
- [x] 7.2 Update `designbook-css-tailwind` skill to handle breakpoints — suggest Tailwind default values (sm: 640px, md: 768px, lg: 1024px, xl: 1280px, 2xl: 1536px), generate `breakpoints` token group, map to Tailwind `screens` config
- [x] 7.3 Update `create-tokens.md` in `designbook-css-tailwind` (framework override) to generate `$type: typography` composite tokens and `breakpoints` group with framework-appropriate names and CSS output
- [x] 7.4 Update `create-tokens.md` in base `designbook-tokens` skill — add `$type: typography` and `breakpoints` to format documentation and full example, update required groups table (both optional, generated by CSS framework skill)
