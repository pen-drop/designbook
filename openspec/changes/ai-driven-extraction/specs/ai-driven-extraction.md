## MODIFIED Requirements

### compare-markup

References: `openspec/specs/ai-driven-extraction/spec.md`

All requirements from the main spec apply to this change. The spec defines the three-phase extraction model, extraction spec format, JSON output format, diff format, severity classification, _debo story check integration, iterative refinement, and polish task consumption.
