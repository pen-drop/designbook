## Tasks

- [x] Create `DeboBadge.jsx` in `packages/storybook-addon-designbook/src/components/ui/`
- [x] Create `DeboCard.jsx` in `packages/storybook-addon-designbook/src/components/ui/` (uses `DeboBadge`)
- [x] Export `DeboBadge` and `DeboCard` from `ui/index.js`
- [x] Export `DeboBadge` and `DeboCard` from top-level `components/index.js`
- [x] Update `DeboDataModel.jsx` — replace `BundleSummary` with `DeboCard`, pass entity type as `badge` prop
- [x] Update `EntityGroup` layout to card grid (`grid-cols-1 md:grid-cols-2`)
- [ ] Verify in Storybook: Data Model page renders cards correctly
- [ ] Verify light and dark themes
