{#
/**
 * @file
 * Template for nav-footer component.
 *
 * Available variables:
 * - items: Array of footer link items with label and url
 */
#}
{% set classes = [
  'component',
  'nav-footer',
] %}

<nav{{ attributes.addClass(classes) }}>
  {% if items %}
    <ul class="nav-footer__list">
      {% for item in items %}
        <li class="nav-footer__item">
          <a href="{{ item.url }}" class="nav-footer__link">{{ item.label }}</a>
        </li>
      {% endfor %}
    </ul>
  {% endif %}
</nav>
