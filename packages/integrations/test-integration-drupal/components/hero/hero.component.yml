name: hero
label: Hero
status: experimental
group: Data Display
thirdPartySettings:
  sdcStorybook:
    disableBasicStory: true
    tags:
      - "!autodocs"
props:
  type: object
  properties:
    variant:
      type: string
      title: Variant
      enum:
        - default
        - landing
      default: default
    title:
      type: string
      title: Heading
    subtitle:
      type: string
      title: Subtitle
    background_color:
      type: string
      title: Background color
slots:
  media:
    title: Media
    description: Hero image or collage
  actions:
    title: Actions
    description: CTA buttons
