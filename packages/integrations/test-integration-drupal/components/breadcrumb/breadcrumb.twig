{# breadcrumb.twig #}
<nav{{ attributes.addClass(['breadcrumb']) }} aria-label="Breadcrumb">
  <ol class="breadcrumb__list">
    {% for item in items %}
      <li class="breadcrumb__item{% if loop.last %} breadcrumb__item--active{% endif %}">
        {% if item.url and not loop.last %}
          <a href="{{ item.url }}" class="breadcrumb__link">{{ item.label }}</a>
        {% else %}
          <span class="breadcrumb__text" aria-current="page">{{ item.label }}</span>
        {% endif %}
      </li>
    {% endfor %}
  </ol>
</nav>
