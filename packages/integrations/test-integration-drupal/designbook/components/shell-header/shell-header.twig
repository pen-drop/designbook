{#
/**
 * @file
 * Template for shell-header component.
 *
 * This is a structural composition — it only renders slots.
 * All HTML markup and styling lives in the referenced UI components.
 *
 * Available slots:
 * - logo: Site branding (renders logo UI component)
 * - navigation: Primary navigation (renders nav-main UI component)
 * - user_menu: User account / language (renders user-menu UI component)
 */
#}
{{ logo }}
{{ navigation }}
{{ user_menu }}
