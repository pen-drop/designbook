$schema: "https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json"
name: entity_node_projects
description: "Project entity. Structural design component."
group: Entity
status: experimental
provider: designbook_design
designbook:
  entity:
    type: node
    bundle: projects
thirdPartySettings:
  sdcStorybook:
    disableBasicStory: true
    tags:
      - "!autodocs"
slots:
  content:
    title: Content
    description: "All project content composed from UI components."
