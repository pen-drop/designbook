/** @config
 {
   "input": "../design-tokens.json",
   "output": "../../css/tokens/font.src.css"
 }
 */
(
  $entries := $each(typography, function($v, $k) {
    "  --font-" & $k & ": \"" & $v."$value" & "\";"
  });
  "@theme inline {\n" & $join($entries, "\n") & "\n}\n"
)
