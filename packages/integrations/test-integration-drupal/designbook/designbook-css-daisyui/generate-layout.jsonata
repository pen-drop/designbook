/** @config
 {
   "input": "../design-tokens.json",
   "output": "../../css/tokens/layout.src.css"
 }
 */
(
  $entries := $each(layout, function($v, $k) {
    "  --layout-" & $k & ": " & $v."$value" & ";"
  });
  "@theme inline {\n" & $join($entries, "\n") & "\n}\n"
)
