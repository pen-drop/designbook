/** @config
 {
   "input": "../design-tokens.json",
   "output": "../../css/tokens/color.src.css"
 }
 */
(
  $entries := $each(color, function($v, $k) {
    "  --color-" & $k & ": " & $v."$value" & ";"
  });
  ":root {\n" & $join($entries, "\n") & "\n}\n"
)
