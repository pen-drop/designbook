import { Meta } from '@storybook/addon-docs/blocks';
import { DeboSection, DeboDesignTokens, DeboMockupWindow } from '../components/index.js';
import { parseMarkdown } from '../components/parsers.js';
import { parse as parseYaml } from 'yaml';
import '../index.css';

<Meta title="Designbook/Design System" />

# Design System

Choose your color palette and typography to define the visual identity of your product.
Use the AI command to select colors and fonts — the result is displayed here automatically.

---

<DeboSection
  title="Design Tokens"
  dataPath="design-system/design-tokens.yml"
  parser={(content) => parseYaml(content)}
  command="/debo-design-tokens"
  emptyMessage="No design tokens defined yet"
  renderContent={(data) => <DeboDesignTokens tokens={data} />}
/>

## Application Shell

Design the persistent navigation and layout that wraps all sections of your application.

<DeboSection
  title="Application Shell"
  dataPath="design-shell/shell-spec.md"
  parser={parseMarkdown}
  command="/design-shell"
  emptyMessage="No shell specification defined yet"
  renderContent={(html) => (
      <DeboMockupWindow>
          <div 
              className="debo-markdown debo:prose debo:prose-sm debo:max-w-none debo:prose-headings:font-normal debo:prose-a:text-primary"
              dangerouslySetInnerHTML={{ __html: html }} 
          />
      </DeboMockupWindow>
  )}
/>

