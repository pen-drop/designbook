config:
  view:
    recent_articles:
      composition: unstructured

content:
  node:
    article:
      fields:
        title:
          type: string
          required: true
        field_body:
          type: text
