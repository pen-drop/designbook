config:
  view:
    recent_articles:
      view_modes:
        default:
          template: view-entity

content:
  node:
    article:
      fields:
        title:
          type: string
          required: true
        field_body:
          type: text
