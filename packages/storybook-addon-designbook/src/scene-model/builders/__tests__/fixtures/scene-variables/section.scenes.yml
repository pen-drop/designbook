group: Test/Section

scenes:
  - name: with-content
    items:
      - scene: shell:shell
        with:
          content:
            - component: test_provider:heading
              props:
                level: h1
              slots:
                text: Hello

  - name: no-with
    items:
      - scene: shell:shell

  - name: deprecated-slots
    items:
      - scene: shell:shell
        slots:
          content:
            - component: test_provider:heading
              props:
                level: h2
              slots:
                text: Deprecated
